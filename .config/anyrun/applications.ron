Config(
  desktop_actions: true,

  max_entries: 5,

  preprocess_exec_script: Some("/home/dan/.config/anyrun/preprocess_commands.sh"),
  terminal: Some(Terminal(
    command: "uwsm",
    args: "app -- kitty {}",
  )),
)
