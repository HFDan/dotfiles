
(defwidget clock []
  (box
    :orientation "v"
    :halign "center"
    :valign "center"
    (label :xalign 0 :text "${formattime(EWW_TIME, "%I:%M %p")}")))

(defwindow clock
  :monitor 0
  :stacking "bg"
  :geometry (geometry :x "0px" :y "50px" :width "100%" :anchor "top center")
  (clock))
